import {typischCoreNop} from "@typisch/core/index";

typischCoreNop();

/**
 * Does nothing, can be used as an early detector for import / module resolution issues.
 */
export function typischEuiNop() {}
